# Spydir

Check if files in a directory have changed.

## Introduction

Calculate the MD5 checksums of all files in the target folder, save the results to a local text file, and then compare the new results with the old ones to detect any changes.
